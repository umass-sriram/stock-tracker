version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
  build:
    commands:
      - cd frontend
      - npm install
      - npm run build
  post_build:
    commands:
      - aws s3 sync build/ s3://stockbrokerapp --delete
artifacts:
  files:
    - '**/*'
